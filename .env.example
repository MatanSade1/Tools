# GCP Configuration
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# Mixpanel Configuration
MIXPANEL_PROJECT_ID=your-mixpanel-project-id

# Mixpanel API Secret (choose one method)
# Option 1: Use Secret Manager (recommended for production)
MIXPANEL_API_SECRET_NAME=mixpanel-api-secret

# Option 2: Use environment variable (less secure, for local testing only)
# MIXPANEL_API_SECRET=your-mixpanel-api-secret

# Slack Configuration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# BigQuery Configuration (optional - defaults shown)
RT_MP_DATASET=mixpanel_data
RT_MP_TABLE=rt_mp_events
